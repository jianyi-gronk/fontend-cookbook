- SCSS（ Sass ）和 Less 都是目前主流的 CSS 预处理器，它们通过变量、嵌套、混入、函数等特性，让 CSS 的编写更加高效和可维护

# 1. 变量定义符

- SCSS 中使用 `$` 定义变量，例如
  ```css
  $primary-color: #333;
  body {
    color: $primary-color;
  }
  ```
- Less 中使用 `@` 定义变量，例如 `@color: red;`
